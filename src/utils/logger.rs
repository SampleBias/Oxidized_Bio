// Logger initialization
// Note: Logging is already initialized in main.rs with tracing

use tracing::info;

pub fn init_logger() {
    // Logging is handled in main.rs
    info!("Logger already initialized via tracing");
}
